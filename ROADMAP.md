# ROADMAP.md — Peta Jalan: ACS Group-Grade, Melampaui GenieACS

Dokumen tracking hidup (living document) untuk membawa ACS ini dari "kerangka arsitektur solid tapi belum teruji" menjadi platform yang benar-benar lebih baik dari GenieACS dari sisi UI/UX, dan layak dipakai oleh **Group** (10+ perusahaan, berbagai vendor CPE) sebagai satu platform bersama.

Update dokumen ini setiap kali sebuah item selesai atau prioritas berubah — jangan biarkan basi. Lihat `PRD.md` untuk requirement produk dan `TECH.md` untuk keputusan arsitektur; dokumen ini adalah **rencana eksekusi**, bukan pengganti keduanya.

**Terakhir diperbarui:** 2026-08-23

---

## 1. Definisi "Lebih Bagus dari GenieACS" (Success Criteria)

Klaim "lebih bagus" harus bisa diukur. Ini baseline pembanding jujur:

| Dimensi | GenieACS (baseline) | Target ACS ini |
|---|---|---|
| UI/UX | Tabel admin utilitarian, tanpa dashboard analitik, navigasi parameter tree mentah | Dashboard visual (tren redaman optik, status device), navigasi berbasis logical key, bulk actions, responsive |
| Multi-tenancy | Tidak native — perlu workaround manual per instance/namespace | Native (`tenants` table, RBAC scope tenant) — target: 10+ perusahaan dalam 1 platform tanpa saling bocor data |
| Provisioning | Script JS bebas per device (powerful tapi rawan, sulit di-review) | Data terstruktur (`vendor_parameter_mappings`, `provisioning_profiles`) — lebih aman & auditable, dengan raw-path fallback |
| Observability | Minim bawaan | Metrics Prometheus + dashboard + alerting backlog task (TECH.md §10) |
| Auditability | Log dasar | Audit trail 7-kolom + activity log + timeline visual per device |
| Kematangan protokol | Bertahun-tahun teruji di lapangan lintas vendor | **Belum teruji sama sekali ke CPE nyata** — ini gap terbesar, lihat Fase 0 |
| Skala terbukti | Production, ratusan ribu device | **Belum ada bukti** — desain stateless tapi belum load-test |

**Kesimpulan jujur:** kita bisa unggul di UI/UX dan model data dalam waktu dekat. Kita **tidak bisa** mengklaim lebih baik secara keseluruhan sampai Fase 0 (validasi dasar) selesai — jangan promosikan ke Group sebelum itu.

---

## 2. Snapshot Kondisi Saat Ini (2026-08-21)

**Sudah ada:**
- Backend Go lengkap untuk 6 usecase inti (session, task, provisioning, device, firmware, diagnostics) + IAM, seluruh REST API FR-1 s/d FR-28 di PRD.md, CWMP handler.
- Frontend React/Tailwind: Devices, Device Detail (parameter/event/task/diagnostics/firmware), Tasks, Provisioning Profiles & Zero-Touch Rules, Firmware catalog, Catalog Vendor (vendor/model/mapping), Administration (user/tenant).
- `docker-compose.yml` untuk stack dev (MariaDB + migrate + acsd).
- 7 subagent proyek di `.claude/agents/` untuk kerja backend (lihat §4).

**Belum tervalidasi / gap kritis — jangan diabaikan:**
- Belum pernah `go build`/`go vet`/`go test` berhasil dijalankan di environment manapun (Go toolchain tidak ada di mesin dev saat dokumen ini ditulis).
- `docker-compose` belum pernah benar-benar dijalankan — migrasi belum pernah dieksekusi ke MariaDB nyata.
- Frontend belum pernah dibuka di browser sungguhan — hanya lulus `tsc -b` + `vite build`.
- Belum ada CPE sungguhan (atau simulator) yang pernah mengirim Inform ke endpoint CWMP ini.
- Belum ada commit git sama sekali — tidak ada baseline yang bisa di-rollback.
- Tidak ada CI/CD — tidak ada gerbang otomatis sebelum kode masuk.
- `GET /vendors/:id/ouis` tidak ada — Catalog UI tidak bisa menampilkan daftar OUI yang sudah didaftarkan.
- Upload firmware hanya mencatat metadata — belum terhubung ke object storage sungguhan (TECH.md §12, belum diputuskan).
- STUN/CGNAT belum ada (memang Fase 2 di PRD).
- Tidak ada subagent yang meng-cover kerja frontend/UX — gap ini ditutup di §4 (agent baru `frontend-ux-builder`).
- Observability (Prometheus/metrics) disebut di TECH.md §10 tapi belum diimplementasikan sama sekali.

---

## 3. Workstream per Fase

Checklist ini dipecah per fase. **Jangan lompat ke Fase 1/2 sebelum Fase 0 selesai** — UI bagus di atas backend yang belum teruji ke CPE nyata adalah rumah bagus di atas fondasi yang belum dicek.

### Fase 0 — Fondasi & Validasi (prasyarat sebelum klaim apa pun ke Group)

- [x] `go build ./...`, `go vet ./...`, `go test ./...` berhasil lulus — divalidasi 2026-08-21 via image `golang:latest` (build ✅, vet ✅ bersih, test ✅ termasuk skenario retry/max_retries di `internal/usecase/task`)
- [x] `docker-compose up` berhasil — divalidasi 2026-08-21: MariaDB healthy, migrasi `0001_init_schema` jalan bersih ("migrate up: selesai"), `acsd` listening di :8080 (REST) dan :7547 (CWMP)
- [ ] Smoke test manual frontend di browser: login, lihat semua halaman baru (Tasks, Provisioning, Firmware, Catalog, Administration), coba satu alur create/edit di masing-masing — **belum bisa dilakukan Claude** (tidak ada tool browser di environment ini), REST API sudah dicek lewat curl/PowerShell tapi interaksi UI sungguhan butuh dicoba manual oleh user
- [x] Commit awal ke git sebagai baseline — commit `6adf25a` (2026-08-21) + `cec904e` fix CWMP auth (2026-08-22), sudah di-push ke `origin/main` atas konfirmasi user
- [x] Setup CI dasar (GitHub Actions) — `.github/workflows/ci.yml` dibuat (job backend: vet+build+test; job frontend: lint+build); divalidasi 2026-08-22 jalan hijau di GitHub sungguhan setelah push ke `origin/main` (run [32545559666](https://github.com/rmdhfz/acs/actions/runs/32545559666)) — kedua job lulus tanpa error
- [x] Uji sesi CWMP end-to-end — divalidasi 2026-08-21 dengan simulasi Inform (event `0 BOOTSTRAP`) manual: InformResponse benar, device ter-upsert, event tercatat, sesi ditutup bersih (POST kosong -> 204, `device_sessions.status=CLOSED`)
- [x] Jalankan audit keamanan menyeluruh ke SEMUA endpoint REST (bukan cuma CWMP/isolasi-tenant yang sudah diaudit sebelumnya) — **selesai 2026-08-23**, lihat detail temuan & perbaikan di bawah §"Audit menyeluruh 2026-08-23"

**[SELESAI 2026-08-22] Endpoint CWMP TIDAK memvalidasi kredensial sama sekali — diperbaiki.**
Root cause: `internal/delivery/cwmp/handler.go` tidak punya pengecekan Basic/Digest Auth apa pun, padahal `TECH.md` §3/§8 dan `PRD.md` FR-1 mensyaratkannya. Ditemukan dengan mengirim Inform simulasi tanpa header `Authorization` — diterima begitu saja.

**Pendekatan yang dipilih & diimplementasikan: shared secret Inform per tenant** (`tenants.cwmp_inform_username`/`cwmp_inform_password_enc`, migrasi `0002_tenant_cwmp_inform_credentials`), dengan kredensial per-device (`devices.inform_username`) sebagai override opsional setelah device dikenal. Anti-spoofing lintas-tenant: bila device sudah py `tenant_id`, secret HARUS milik tenant yang sama. Superadmin kelola shared secret via `POST /tenants` (saat create) atau `PATCH /tenants/:id/cwmp-credentials` (rotate) — sudah ada UI-nya di Administration > Tenants.

**Bonus fix sekalian:** device yang dibuat dari Inform sekarang otomatis dapat `tenant_id` dari tenant yang match kredensialnya — sebelumnya `FindOrCreateFromInform` TIDAK PERNAH meng-assign tenant_id sama sekali (device baru selalu `tenant_id=NULL`, artinya admin/NOC ber-scope-tenant tidak akan pernah melihat device barunya sendiri — bug fondasional untuk use-case Group).

**Divalidasi end-to-end manual** (docker-compose + MariaDB nyata, bukan cuma unit test) untuk 5 skenario: (1) tanpa auth → 401, (2) shared secret benar + device baru → 200 & tenant_id ter-assign otomatis, (3) password salah → 401, (4) device tenant A coba pakai secret tenant B → 401 (anti-spoofing), (5) device dgn override per-device wajib pakai kredensial override-nya, bukan shared secret tenant → 401 kalau salah. `go build`/`vet`/`test` dan `npm run build` lulus semua setelah perubahan.

**Independent review (`acs-code-reviewer` + `acs-security-reviewer`) atas fix di atas — semua temuan signifikan sudah ditindaklanjuti (2026-08-22):**
- **[KRITIS, DIPERBAIKI]** Device lama/orphan (`tenant_id NULL`) tidak ter-lindungi anti-spoofing DAN tidak pernah "sembuh" — celah re-terbuka tiap Inform. Fix: `FindOrCreateFromInform` sekarang meng-assign `tenant_id` begitu device orphan berhasil Inform dgn kredensial tenant manapun, lalu klaim **terkunci** (tenant lain langsung ditolak sesudahnya) — divalidasi live: device orphan `SIMTEST-0001` sembuh ke `tenant_id=1` pada Inform pertama, lalu klaim dari tenant lain ditolak 401 di percobaan berikutnya.
- **[TINGGI, DIPERBAIKI]** Tidak ada rate limiting di `/cwmp` padahal sekarang menjaga secret sungguhan. Fix: `middleware.RateLimiter` 5 req/s per identifier di `cwmpEcho` — divalidasi live (burst 15 request memicu beberapa `429`).
- **[TINGGI, DIPERBAIKI]** Tidak ada validasi panjang minimum `cwmp_inform_password`. Fix: minimal 16 karakter di `iam.Service` — divalidasi live (password 8 karakter ditolak `400`).
- **[SEDANG, DIPERBAIKI]** Kredensial override per-device tidak ikut tercabut saat tenant pemiliknya dinonaktifkan. Fix: `authenticateInform` sekarang cross-check `tenant.IsActive` juga di jalur override (unit test).
- **[SEDANG, DIPERBAIKI — code review]** Query `devices` duplikat di setiap Inform (hot path). Fix: `authenticateInform` meneruskan device yang sudah di-fetch ke `FindOrCreateFromInform` lewat `ExistingDevice`, bukan query ulang.
- **[SEDANG, DIPERBAIKI — code review]** Logic keamanan baru (`authenticateInform`/`passwordMatches`) tanpa unit test. Fix: `internal/usecase/session/service_test.go` ditambahkan, 11 skenario (anti-spoofing, override wajib, tenant nonaktif, orphan healing, password kosong, dst) — semua lulus.
- **[RENDAH, DITERIMA APA ADANYA]** Timing side-channel minor (durasi respons beda antara username tak dikenal vs password salah) dan perbandingan username tidak constant-time — severity rendah menurut reviewer sendiri, mitigasi penuh butuh dummy-crypto-ops yang menambah kompleksitas tidak sepadan untuk saat ini. Dicatat, tidak diperbaiki.
- **[FOLLOW-UP, bukan blocker]** Tidak ada endpoint REST untuk SET kredensial per-device (`devices.inform_username/inform_password_enc`) — kolom & logic override-nya sudah ada dan tervalidasi jalan lewat unit test + DB manual, tapi baru bisa diisi manual di DB, belum ada jalur API/UI. Tambahkan ke Fase 2 kalau override per-device memang dibutuhkan operasional.
- **[SELESAI 2026-08-22, commit `0ac772a`]** Rate limiting REST API internal (`restEcho`) — sudah ditambahkan, 30 req/s per-IP.
- **[FOLLOW-UP, bukan blocker]** Tidak ada endpoint untuk menonaktifkan tenant (hanya create/list) — jadi skenario "device override milik tenant nonaktif ditolak" baru tervalidasi lewat unit test, belum lewat REST end-to-end (butuh endpoint `PATCH /tenants/:id` untuk toggle `is_active` dulu).

*Agent terkait: `cwmp-session-engineer` (uji sesi & auth), `db-schema-guardian` (validasi migrasi), `acs-security-reviewer` + `acs-code-reviewer` (review independen), kerja CI/commit dilakukan langsung bersama user.*

**[SELESAI 2026-08-22, commit `5ec6a23`] `GET /tasks`, `GET /tasks/:id`, `POST /tasks/:id/cancel` sebelumnya sama sekali tidak tenant-scoped.**
Ditemukan `acs-code-reviewer` saat review Dashboard (bukan regresi dari situ — gap sudah ada sebelumnya, jadi lebih kentara karena endpoint baru `GET /tasks/stats` justru sudah benar tenant-scoped). Dampak sebelum fix: user non-superadmin tenant mana pun bisa membaca DAN **membatalkan** (`Cancel` menerima `actor` tapi tidak pernah memvalidasinya — ditemukan sendiri saat mengerjakan fix ini, bukan dari review) task tenant lain, termasuk `tasks.parameters` yang bisa memuat kredensial CPE (WiFi passphrase/PPPoE) yang sedang di-push, dengan ID sequential yang gampang ditebak.

Fix: `TaskFilter` dapat `TenantID` (resolve via JOIN devices), `task.Service.List/Get/Cancel` sekarang terima `actor` + terapkan `requireTaskTenantScope` (pola sama seperti `device.Service.requireTenantScope`). Divalidasi live dgn 2 user ADMIN tenant berbeda: tenant lain dapat 403 di GET & cancel task tenant lain, list-nya kosong (bukan bocor), pemilik tenant tetap bisa akses normal, superadmin tetap lintas-tenant.

**[SELESAI 2026-08-23] Audit menyeluruh 2026-08-23 — dipicu persiapan uji fisik CPE hari Senin (lihat `PENGUJIAN_LAPANGAN.md`).** `acs-security-reviewer` mengaudit seluruh endpoint REST yang belum eksplisit disebut di audit-audit sebelumnya (device sub-resource, provisioning/ZTP, catalog vendor, auth). Ditemukan 1 **KRITIS**, 1 **TINGGI**, beberapa **SEDANG** — semua diperbaiki & divalidasi live sebelum commit:

- **[KRITIS, DIPERBAIKI]** `POST /users` (`iam.Service.CreateUser`, sudah ada sejak awal) tidak pernah memvalidasi `role_codes` terhadap level actor — ADMIN tenant mana pun bisa `POST /users` dengan `role_codes:["SUPERADMIN"]` dan berhasil membuat akun superadmin permanen, bypass total isolasi tenant. Fix awal (helper `validateRoleCodesAssignable`, dipakai di `CreateUser` dan endpoint baru `ReplaceUserRoles`) sempat **bisa dilewati lagi** oleh audit independen kedua — exact-case string check (`slices.Contains(roleCodes, "SUPERADMIN")`) gagal menangkap `role_codes:["superadmin"]` (huruf kecil), padahal kolom `ref_roles.code` pakai kolasi `utf8mb4_unicode_ci` yang case-INSENSITIVE, jadi `GetByCode("superadmin")` tetap resolve ke role SUPERADMIN asli. Fix final: `strings.EqualFold` bukan exact-case compare. Divalidasi live: `role_codes:["superadmin"]` DAN `role_codes:["SuperAdmin"]` sama-sama ditolak 403, akun tidak pernah dibuat/ter-assign.
- **[TINGGI, DIPERBAIKI]** Pola fail-open "nil tenant_id = tanpa filter WHERE" di `device.Service.{List,Stats}`, `task.Service.{List,Stats}`, `provisioning.Service.{List,ListZeroTouchRules}` (yang terakhir logic-nya awalnya malah di HANDLER, bukan usecase — pelanggaran Clean Architecture sekalian diperbaiki), **dan** `iam.Service.ListUsers` (celah identik, sempat kelewat di ronde fix pertama, ketemu di ronde audit kedua). Skenario: superadmin bikin user non-superadmin (ADMIN/NOC) tanpa `tenant_id` (tidak ada validasi yang mencegah ini) → user itu login dgn `actor.TenantID=nil` → `GET /devices`, `/tasks`, `/provisioning-profiles`, `/zero-touch-rules`, `/users` semua balikin data SELURUH TENANT. Fix: helper baru `auth.ScopedTenantFilter(actor)` — non-superadmin dgn `TenantID=nil` ditolak (403), bukan diam-diam dianggap "tanpa filter". Divalidasi live di kelima endpoint dgn user ADMIN sengaja dibuat tanpa tenant_id — semua 403, tidak ada satu pun 200-dengan-data-lintas-tenant.
- **[SEDANG, DIPERBAIKI]** Bug turunan dari fix di atas (ditemukan sendiri, bukan dari review): diff assign/revoke role di `ReplaceUserRoles` membandingkan `role_codes` request (casing bebas) terhadap `target.Roles` dari DB (selalu kanonik) secara exact-case — mengirim role yang SAMA cuma beda casing (mis. `"admin"` padahal DB simpan `"ADMIN"`) membuat diff keliru mengira role itu "dicabut" lalu benar-benar me-revoke-nya. Fix: resolve semua `role_codes` ke bentuk kanonik dari DB dulu sebelum diff. Divalidasi live: replace roles pakai `["admin"]` huruf kecil pada user yang sudah py role `ADMIN` → role TETAP ada (tidak hilang).
- **[SEDANG, DIPERBAIKI]** Audit trail (FR-29) bolong di `provisioning.Service.{UpdateProfile,DeleteProfile}` dan seluruh CRUD `ZeroTouchRule` (tidak pernah panggil `activity.Record`), plus 4 handler catalog vendor (`createVendor`/`addVendorOUI`/`createDeviceModel`/`upsertParameterMapping`) yang langsung panggil repository tanpa lapisan apa pun (tanpa jejak siapa mengubah data vendor global lintas-tenant). Fix: tambah `activity.Record` di titik-titik itu (utk catalog, lewat field baru `Router.Activity` — **catatan jujur:** ini activity-log dipanggil langsung dari handler, bukan lewat usecase, krn `catalog_handler.go` memang belum py lapisan usecase sama sekali (debt pra-eksisting, dicatat `acs-code-reviewer`, BUKAN diperbaiki penuh di sesi ini — follow-up: buat `catalog.Service` tipis kalau mau konsisten penuh dgn pola Clean Architecture di modul lain). Divalidasi live: create vendor/profile/update/delete masing-masing menghasilkan baris `activity_logs` yang benar.
- **[SEDANG, DITERIMA — followup]** `acs-code-reviewer` menemukan duplikasi logic tenant-scope (`requireTenantScope` lokal identik di `device`/`provisioning`/`task`, padahal `auth.RequireTenantScope` kanonik sudah ada) — dikonsolidasi SEBAGIAN (device & provisioning sekarang reuse `auth.RequireTenantScope`, duplikat lokalnya dihapus), `task.Service` masih py versi inline sendiri (`requireTaskTenantScope`) — dibiarkan apa adanya, follow-up bukan blocker.
- **[RENDAH, DITERIMA APA ADANYA]** `GET /devices/:id` mengembalikan `connection_request_username` plaintext ke semua role termasuk VIEWER (password-nya sudah aman, terenkripsi & `json:"-"`); pola "403 vs 404" di endpoint mutasi bisa dipakai enumerasi ID resource tenant lain tanpa membocorkan isinya (pre-existing, bukan regresi baru). Keduanya severity rendah menurut reviewer, tidak diperbaiki, dicatat sbg debt.

*Agent terkait di audit ini: `acs-security-reviewer` (2 ronde independen), `acs-code-reviewer` (1 ronde) — kedua ronde review menemukan hal yang beda, saling melengkapi. Fix diimplementasikan langsung oleh koordinator (bukan subagent) untuk device/task/provisioning/catalog; fitur self-service (lihat Fase 2) dikerjakan `rest-api-builder` terpisah dan menyumbang sebagian file yang sama.*

### Fase 1 — Diferensiasi UI/UX (bagian yang bikin "lebih bagus dari GenieACS" terasa nyata)

- [x] Dashboard analitik — **selesai 2026-08-22** (commit `d997007`): halaman `/dashboard` baru (landing page pengganti `/devices`) dgn distribusi status device, device per vendor, antrean task per status (Recharts), plus tab "Redaman Optik" (tren RX/TX power per device) di Device Detail. Backend: `GET /devices/stats`, `GET /tasks/stats` (aggregate, tenant-scoped). Direview `acs-code-reviewer` — bersih, routing aman, tenant-scoping benar. **Belum tercakup:** tren online/offline dari waktu ke waktu (historis) — butuh tabel snapshot periodik baru yang sengaja belum dibuat karena interval/retensi belum dikonfirmasi ke user (lihat TECH.md §12 soal partisi tabel log); "agregat redaman optik per tenant" (lintas device) juga belum ada, baru per-device.
- [x] Bulk actions — **selesai 2026-08-22** (commit `cf8d4dc`): checkbox seleksi multi-device di DevicesPage (scoped ke halaman/filter aktif), toolbar aksi (apply profile/firmware/reboot) gated role ADMIN/NOC. Murni frontend — memanggil endpoint per-device yang sudah ada secara sekuensial, device vendor tidak cocok otomatis dilewati & ditandai di hasil. Tidak ada perubahan backend, jadi tidak direview `acs-code-reviewer` terpisah (endpoint yang dipanggil sudah pernah diaudit).
- [x] Provisioning profile builder dengan parameter picker — **selesai 2026-08-22** (commit `ee5869a`): autocomplete `logical_key` dari `vendor_parameter_mappings` (datalist native, di-scope ke vendor profil), preview `tr069_path` hasil resolve di bawah tiap baris. Raw path manual tetap didukung (convenience layer, TECH.md §5).
- [x] Global search / command palette — **selesai 2026-08-22** (commit `ee5869a`): Ctrl/Cmd+K dari mana saja, cari device (serial/MAC, debounce 250ms) + quick-nav semua halaman (role-aware). Otomatis lintas-tenant utk superadmin / dalam-tenant utk role lain karena reuse endpoint `/devices?search=` yang sudah tenant-scoped di backend.
- [x] Notifikasi in-app — **selesai 2026-08-22**: bell icon + panel dropdown, deteksi client-side dari polling yang sudah ada (`useTaskStats`/`useDeviceStats`, refetch 5 detik) — task baru gagal, device baru offline (delta naik), backlog PENDING lewat ambang 20. **Jujur soal batasannya:** ini POLLING, bukan WebSocket/push sungguhan — belum ada infrastruktur realtime di backend. Cukup terasa real-time utk skala NOC kecil-menengah tapi bukan pengganti event stream sungguhan bila kebutuhan jadi lebih berat (lihat catatan di `lib/notifications.tsx`).
- [x] Timeline audit trail visual per device — **selesai 2026-08-22** (commit `ee5869a`): tab baru "Timeline Audit" di Device Detail, endpoint baru `GET /devices/:id/activity` (tenant-scoped), `ActivityLogRepository.ListByEntity` di-LEFT JOIN ke `users` utk resolve username. Scoped ke `entity_type="device"` — melengkapi (bukan duplikasi) tab Histori Event (CWMP) & Task (RPC) yang sudah ada.
- [x] Dark mode — **selesai 2026-08-22**: toggle 3-state (sistem/terang/gelap) di top bar, persisted localStorage, `@custom-variant dark` class-based (Tailwind v4). **Cakupan:** seluruh komponen bersama (Layout/sidebar, Modal, StatusBadge, StatCard, EmptyState, Spinner, CommandPalette, NotificationBell), pola berulang di semua halaman (card container, table header, divide row, row hover, `inputCls`/`primaryBtnCls`), teks heading/body/secondary di semua halaman, dan chart Recharts (Dashboard + Redaman Optik) theme-aware. **Jujur soal batasannya:** dikerjakan tanpa akses browser sama sekali — divalidasi lewat audit tekstual manual utk konflik aturan `dark:` yang saling tabrakan (ditemukan & diperbaiki 5 kasus nyata: `hover:`/`placeholder:` yang kehilangan prefix variant-nya), BUKAN lewat screenshot visual. Kemungkinan ada elemen kecil yang terlewat (mis. warna hover pada elemen yang jarang dipakai) — tolong coba toggle-nya dan laporkan kalau ada yang masih terang/kontrasnya jelek.
- [x] Tampilan mobile-responsive — **selesai 2026-08-22**: sidebar jadi drawer off-canvas di layar <768px (hamburger toggle, top bar mobile terpisah dgn tombol tema+notifikasi), auto-close saat pindah halaman. **Cakupan lebih luas dari yang diminta** (ROADMAP awal minta "device detail ringkas utk teknisi lapangan" — dikerjakan malah utk seluruh app shell, karena strukturnya (sidebar tetap) memang blocker utama mobile, bukan spesifik device detail). Tabel lebar (mis. daftar device/task) belum diverifikasi scroll horizontal dgn baik di layar sempit — sama seperti dark mode, ini belum diuji visual di browser sungguhan.
- [x] Wizard onboarding tenant baru — **selesai 2026-08-22** (commit `ee2cdff`): `/administration/onboarding`, 3 langkah (tenant + shared secret CWMP opsional → admin pertama → ringkasan + CTA ke Provisioning/Catalog). Superadmin-only dgn guard di halaman itu sendiri (bukan cuma sembunyi dari nav). Reuse endpoint yang sudah ada, tanpa backend baru. **Beda dari rencana awal:** langkah "pilih vendor default" diganti jadi CTA opsional ke halaman Provisioning/Catalog di step akhir alih-alih step wajib tersendiri — karena vendor adalah data katalog global (bukan per-tenant), assign vendor "default" utk satu tenant sebenarnya berarti buat Provisioning Profile/Zero-Touch Rule, yang sudah py builder sendiri di halaman lain; memaksakannya jadi step wizard akan duplikasi UI yang sudah ada.

**Fase 1 SELESAI SEMUA** (2026-08-22) — 9 item, semua di-commit & push, CI hijau tiap commit. Ringkasan capaian: Dashboard analitik (Recharts), Bulk Actions, Parameter picker provisioning, Command palette (Ctrl/Cmd+K), Timeline audit trail, Notifikasi in-app (polling), Dark mode, Layout mobile-responsive, Wizard onboarding tenant. Bonus: 1 celah keamanan kritis (CWMP tanpa auth) + 1 celah isolasi tenant tinggi (`GET/POST /tasks/*`) ditemukan & diperbaiki di tengah jalan, semua direview independen oleh `acs-code-reviewer`/`acs-security-reviewer`.

**Keterbatasan yang jujur perlu dicatat sebelum lanjut Fase 2:** dark mode dan mobile-responsive **belum pernah diverifikasi visual di browser sungguhan** (tidak ada tool browser di environment Claude Code ini) — divalidasi lewat type-check, lint, build, dan audit tekstual manual thd konflik CSS. Notifikasi in-app berbasis polling, bukan push/WebSocket sungguhan. Tolong coba semuanya manual (`npm run dev`) dan laporkan kalau ada yang terlihat aneh.

*Agent terkait: `frontend-ux-builder` (baru, lihat §4), `rest-api-builder` (endpoint agregasi/statistik bila backend belum expose data yang dibutuhkan chart).*

**Belum diputuskan — konfirmasi dulu sebelum dikerjakan:** pilihan library charting (mis. Recharts vs visx vs D3 manual) belum ditentukan; jangan pilih sepihak, tanyakan preferensi (ukuran bundle vs fleksibilitas) sebelum epic pertama di fase ini dimulai.

### Fase 2 — Enterprise / Multi-Tenant untuk Skala Group (10+ perusahaan)

**Fase 2 SELESAI SEMUA** (2026-08-23) — 8 item. Setiap item non-trivial direview independen `acs-code-reviewer`+`acs-security-reviewer` sebelum commit; beberapa temuan tinggi/kritis ditemukan & diperbaiki di sepanjang jalan (lihat detail per item di bawah dan di §"Audit menyeluruh 2026-08-23" Fase 0). Item terakhir (kuota task queue, observability Prometheus, object storage MinIO) dikerjakan berurutan (bukan paralel) krn saling bersinggungan file (`router.go`, `main.go`, `docker-compose.yml`) — pelajaran dari insiden `docker compose down -v` di awal sesi ini.

- [x] White-labeling per tenant (logo, nama produk, warna aksen) di `Layout.tsx` — **selesai 2026-08-23**: migrasi `0003_tenant_branding` (kolom `brand_name`/`logo_url`/`primary_color` nullable di `tenants`), `GET /tenants/current` (semua role terautentikasi, DTO sempit) + `PATCH /tenants/:id/branding` (superadmin utk tenant manapun, ADMIN self-service tenant sendiri saja). `Layout.tsx` render brand name/logo/warna aksen di sidebar + top bar mobile, fallback ke "ACS Console" default. UI: tab "Branding" di Administration utk ADMIN non-superadmin, tombol "Branding" di tabel Tenants utk superadmin.

  Direview `acs-code-reviewer` — **1 temuan tinggi + 3 sedang, semua diperbaiki sebelum commit:** (1) **[TINGGI]** `GET /tenants/current` awalnya mengembalikan seluruh struct `domain.Tenant` termasuk `cwmp_inform_username` (separuh shared secret Inform CWMP) ke SEMUA role terautentikasi termasuk NOC/VIEWER — diperbaiki dgn DTO respons sempit (`currentTenantResponse`/`CurrentTenant` FE, hanya `id/name/brand_name/logo_url/primary_color`); (2) **[SEDANG]** tidak ada validasi server-side utk `primary_color`/`logo_url`/`brand_name` — payload yg melebihi lebar kolom bisa jatuh jadi 500 generik alih-alih 400 — diperbaiki dgn `validateBranding` (regex hex color, scheme http/https wajib utk logo URL, batas panjang eksplisit); (3) **[SEDANG]** warna teks nav aktif di-hardcode putih saat tenant set warna aksen custom — tenant yg pilih warna terang (mis. `#ffffff`) bikin teks nav aktif tidak terbaca — diperbaiki dgn `accentTextClass` (hitung kontras YIQ, pilih teks hitam/putih otomatis); (4) **[SEDANG]** `RowsAffected` dari UPDATE tidak dicek, jadi `PATCH` ke tenant ID yang tidak ada tetap balas 204 — diperbaiki dgn existence-check terpisah sebelum UPDATE (bukan pakai `RowsAffected`, krn driver mysql tidak menghitung baris yg match tapi nilainya sama — submit ulang branding identik jangan sampai keliru dianggap 404). Duplikasi logic RBAC (`UpdateBranding` vs `auth.RequireTenantScope`) juga dikonsolidasi. 1 temuan rendah (RowsAffected tidak dicek di `SetCWMPInformCredentials` yang sudah ada sebelumnya) dicatat tapi tidak diperbaiki — pre-existing, di luar scope perubahan ini.

  **Divalidasi end-to-end** ke MariaDB nyata (docker-compose): migrasi jalan bersih 2x (sebelum & sesudah fix review), kolom terkonfirmasi via `DESCRIBE`. Live REST test lengkap: superadmin PATCH tenant manapun (204), ADMIN self-service tenant sendiri (204), ADMIN lintas-tenant ditolak (403, DB terkonfirmasi tenant lain tidak berubah), NOC bisa baca (`GET /tenants/current` — DTO sempit terkonfirmasi tidak ada `cwmp_inform_username`/`tenant_uuid`/audit fields) tapi ditolak nulis (403), validasi hex color/URL scheme/panjang (400 utk masing2 kasus invalid), tenant ID tidak ada (404, bukan 404-palsu krn RowsAffected), resubmit branding identik tetap 204 (bukan 404-palsu). `go vet`/`build`/`test` dan `npm run build` lulus. Data uji dibersihkan dari DB dev setelahnya.

  **Batasan yang jujur:** belum dicoba visual di browser sungguhan (tidak ada tool browser di environment ini) — tolong buka Administration > Branding (sbg ADMIN tenant) dan Administration > Tenants > tombol Branding (sbg superadmin) utk konfirmasi tampilannya oke, termasuk cek warna aksen custom di sidebar/nav.
- [x] Tenant admin self-service penuh: kelola user & role tenant sendiri tanpa perlu superadmin turun tangan — **selesai 2026-08-23** (dikerjakan `rest-api-builder`, direview independen `acs-code-reviewer`+`acs-security-reviewer`, temuan kritis diperbaiki — lihat detail di §"Audit menyeluruh 2026-08-23" Fase 0). 4 endpoint baru: `PATCH /users/:id` (update profil + toggle aktif), `PATCH /users/:id/password` (admin-reset, bukan self-service ubah password sendiri — itu di luar scope ini), `PATCH /users/:id/roles` (full-replace, dgn resolusi role ke bentuk kanonik DB), `DELETE /users/:id` (soft delete). RBAC: superadmin bebas, ADMIN dibatasi tenant sendiri (`auth.RequireTenantScope`) + guard self-lockout (tidak bisa nonaktifkan/hapus/cabut role ADMIN dari akun sendiri) + guard privilege-escalation (tidak bisa assign role SUPERADMIN ke siapa pun, termasuk lewat variasi casing — lihat detail bug case-insensitivity yang sempat lolos di §"Audit menyeluruh 2026-08-23"). Divalidasi live lengkap: update/reset-password/roles/delete lintas skenario in-tenant/cross-tenant/self-lockout/escalation, semua sesuai ekspektasi (204/403/404 di tempat yang benar). **Batasan:** tidak ada UI frontend baru untuk endpoint ini di sesi ini (backend-only) — Administration > Users di frontend masih read-only + create-user saja, belum ada tombol edit/reset-password/roles/delete. Follow-up FE terpisah kalau dibutuhkan.
- [x] Uji isolasi tenant end-to-end — **selesai 2026-08-22** (commit `0ac772a`): audit sistematis oleh `acs-security-reviewer` ke SELURUH endpoint REST (bukan spot-check). Ketemu **3 celah kritis** (eskalasi privilese lintas tenant via `POST /auth/tokens`, `GET /provisioning-profiles/:id` tanpa scope sama sekali, `apply-profile` tidak validasi tenant milik profile), 1 tinggi (`firmware-jobs` tanpa scope), 1 sedang (upload firmware harusnya superadmin-only). Semua diperbaiki & divalidasi live dgn 2 tenant + superadmin. Bonus: ketemu & perbaiki bug lama tak terkait (struct `ProvisioningProfileParameter` bikin endpoint itu selalu 500).
- [x] `GET /vendors/:id/ouis` + tampilan Catalog UI — **selesai 2026-08-22** (commit `0ac772a`): endpoint baru + kolom "OUI Terdaftar" di tabel Vendors.
- [x] Rate limiting REST API internal — **selesai 2026-08-22** (commit `0ac772a`): 30 req/s per-IP di `restEcho` (vs 5 req/s di `/cwmp`, lebih longgar krn dashboard polling tiap 5 detik). Menutup gap yang dicatat sejak rate limit CWMP ditambahkan.
- [x] Kuota **task queue** per tenant (bukan cuma per-IP global) — **selesai 2026-08-23** (dikerjakan `db-schema-guardian`, direview `acs-code-reviewer`+`acs-security-reviewer`). Kolom baru `tenants.max_pending_tasks` (nullable, NULL = tidak dibatasi); `task.Service.CreateTask` menolak (429, `domain.ErrQuotaExceeded`) task baru kalau tenant device target sudah punya task PENDING/QUEUED >= batas — dicek lewat `TaskRepository.CountPendingForTenant` (query COUNT murni, bukan `List()` yang ikut fetch baris lengkap). Endpoint `PATCH /tenants/:id/task-quota`, **superadmin-only** (beda dari branding yang self-service ADMIN — ini kebijakan platform, bukan tenant). Enforcement berlaku di SATU titik masuk (`CreateTask`) yang dipakai semua jalur (REST manual, firmware upgrade, diagnostics, ZTP via `ApplyProfile`) — diverifikasi tidak ada jalur bikin task yang lolos. Test unit `TestCreateTaskTenantQuota` (5 skenario: tanpa kuota, di bawah batas, tepat batas, device orphan, error propagation), diverifikasi lewat mutation-testing (sengaja dirusak dulu, konfirmasi test gagal sebagaimana mestinya, lalu dikembalikan) — bukan cuma "lulus karena tidak menguji apa-apa".

  **Bug turunan yang ditemukan & diperbaiki sebelum commit:** rule ZTP yang cocok tapi gagal di-apply (mis. kena kuota) sebelumnya GAGAL SENYAP — bertentangan dgn FR-15 ("device tidak boleh silently diabaikan"). `EvaluateZeroTouch` sekarang mencatat `activity_logs` (`ZERO_TOUCH_APPLY_FAILED`) saat rule cocok tapi apply gagal, supaya NOC bisa lihat kenapa device tidak ter-provisioning otomatis.

  **Diterima apa adanya (bukan blocker):** TOCTOU minor pada hitungan kuota (cek-lalu-insert bukan operasi atomik, tanpa lock) — request paralel bisa membuat pending sedikit melebihi batas. Severity rendah menurut `acs-security-reviewer` DAN `acs-code-reviewer` (bukan boundary keamanan, murni kuota lunak); tidak diperbaiki dgn transaksi/lock supaya tidak overengineer fitur ini.

  **Scope sengaja dibatasi:** ini HANYA kuota task queue, BUKAN rate limit koneksi/sesi CWMP itu sendiri (CWMP sesi stateful, perubahan di area itu lebih invasif, di luar cakupan sesuai CLAUDE.md).
- [x] Playbook "cara menambah vendor baru" — **selesai 2026-08-22**: `VENDOR_ONBOARDING.md` — panduan operasional step-by-step via Catalog Vendor UI. **Sesuai keputusan user:** dokumentasi saja, TANPA sistem request/approval baru (superadmin tetap eksekusi langsung).
- [x] Object storage firmware — **selesai 2026-08-23** (dikerjakan `rest-api-builder`, direview `acs-code-reviewer`+`acs-security-reviewer`). `POST /firmware` sekarang terima file sungguhan (multipart, sebelumnya cuma JSON metadata dgn `file_path` string bebas dari client — TIDAK ADA file yang benar-benar diterima ACS). File diupload ke MinIO, checksum SHA-256 dihitung SERVER-SIDE saat streaming (client tidak lagi bisa kirim checksum palsu). Kolom `firmware_files.file_path` di-rename `storage_key` (migrasi `0007_firmware_storage_key`) supaya maknanya jelas: object key MinIO, bukan path filesystem. **Strategi delivery ke CPE (keputusan user): presigned GET URL LANGSUNG dari MinIO** (bukan proxy lewat ACS) — masa berlaku 1 jam, digenerate on-demand saat `ScheduleUpgrade`, tidak pernah disimpan. **Asumsi deployment yang disadari sepenuhnya:** MinIO harus reachable dari jaringan CPE fisik, bukan cuma dari ACS/dashboard.

  **Ditemukan & diperbaiki review sebelum commit:**
  - **[SEDANG-TINGGI, DIPERBAIKI]** Presigned URL (bearer-token-like — siapa pun yang pegang bisa download tanpa auth lain sampai kedaluwarsa) awalnya bocor ke role rendah lewat endpoint task generik: `GET /tasks`/`GET /tasks/:id` sengaja tidak digating role (NOC/VIEWER boleh lihat status task utk monitoring), tapi itu berarti VIEWER pun bisa lihat URL presigned mentah di `task.Parameters` lalu meneruskannya keluar organisasi. Fix: `task.Service.List/Get` sekarang redact field `url`/`username`/`password` di `Parameters` (ganti `"[redacted]"`) untuk role selain ADMIN/SUPERADMIN — NOC/VIEWER tetap lihat status & metadata task, tidak lihat kredensial/URL-nya. Divalidasi live: NOC & VIEWER dapat `"[redacted]"`, superadmin tetap dapat URL asli yang valid (diverifikasi bisa di-GET & isinya cocok).
  - **[SEDANG, DIPERBAIKI]** Race condition auto-create bucket MinIO saat beberapa instance `acsd` start bersamaan (app server didesain stateless/multi-instance, TECH.md §9) — instance yang kalah race dapat error dari MinIO lalu `log.Fatalf` saat startup. Fix: error `BucketAlreadyOwnedByYou`/`BucketAlreadyExists` dari `MakeBucket` diperlakukan sbg sukses, bukan kegagalan.
  - **[RENDAH, DIPERBAIKI]** Bagian nama file di object key tidak dibatasi panjangnya (dikontrol penuh client) — ditambah batas 100 karakter.
  - **[RENDAH, DIPERBAIKI]** Migrasi `0007` (rename kolom) tidak mendokumentasikan implikasi kalau dijalankan di instance yang sudah punya data lama dari flow metadata-only — ditambah catatan eksplisit di file migrasi (tidak actionable sekarang, `schema.sql` tidak seed data `firmware_files`).
  - **[RENDAH, DIPERBAIKI]** 2 jalur error penting (`storage.Upload` gagal, `storage.PresignedGetURL` gagal) di-scaffold di test tapi tidak pernah dipicu — ditambah `TestUploadFirmware_PropagatesStorageUploadError` dan `TestScheduleUpgrade_PropagatesPresignError`.
  - **[RENDAH, DITERIMA APA ADANYA]** Kalau proses crash tepat di antara `storage.Upload` sukses dan `files.Create`/cleanup sempat jalan, objek yatim tertinggal permanen di bucket (resource leak, bukan kebocoran data/akses). Tidak ada job reconciliation/orphan-sweeper — butuh keputusan desain tambahan, dicatat sbg follow-up bukan blocker.

  **Divalidasi end-to-end nyata** (docker-compose + MinIO sungguhan, bukan mock): upload file dummy → 201, checksum di response cocok `sha256sum` file asli; jadwalkan upgrade → task Download RPC terbentuk dgn presigned URL; **URL di-GET langsung dari dalam docker network (tanpa modifikasi apa pun, krn signature SigV4 terikat ke hostname persis) → berhasil, isi file yang diunduh identik dgn yang diupload**; RBAC upload tetap superadmin-only; file > 256MB ditolak cepat sebelum baca body penuh; redaksi URL terkonfirmasi per-role.
- [x] Observability: metrics Prometheus (device online/offline, task queue depth per status, error rate per vendor) + alert saat backlog menumpuk (TECH.md §10) — **selesai sebagian, 2026-08-23** (dikerjakan `rest-api-builder`, direview `acs-code-reviewer`). Endpoint baru `GET /api/v1/metrics` (SENGAJA tanpa auth — konvensi Prometheus exporter, wajib difirewall di level jaringan produksi, bukan aplikasi), custom Collector query-on-scrape (`internal/metrics/`) reuse usecase yang sudah ada (bukan query repository langsung). 6 metrik: `acs_devices_by_status`, `acs_devices_by_vendor`, `acs_task_queue_depth`, `acs_task_completion_seconds_avg`, `acs_task_failed_current` (per vendor — awalnya dinamai `acs_task_errors_total`, DIGANTI setelah review krn ini gauge bukan counter, nama `_total` bisa memancing orang salah pakai `rate()`/`increase()`), `acs_cwmp_sessions_open`. `docker-compose.yml` dapat service `prometheus` baru (`deploy/prometheus.yml` scrape config, `deploy/prometheus_alerts.yml` 2 rule: backlog PENDING > 50 selama 5 menit, task FAILED per vendor > 10).

  **Diperbaiki setelah review kedua (`acs-code-reviewer`) sebelum commit:**
  - **[TINGGI]** Query metrik baru (hitung sesi CWMP terbuka, rata-rata waktu selesai task, error per vendor) dipanggil tiap scrape (default 15 detik) **selamanya** tanpa index pendukung — `device_sessions.status` sama sekali tidak punya index (tabel ini tumbuh terus seiring Inform CWMP, mirip `device_events`), `tasks.completed_at` dan `tasks.task_status_id` (berdiri sendiri, bukan cuma composite yang diawali `device_id`) juga tidak ada. Fix: migrasi baru `0006_task_metrics_indexes` menambah ketiga index tsb — divalidasi live (`SHOW INDEX`).
  - **[SEDANG]** Pola "actor sintetis ber-role SUPERADMIN" (`platformActor`) dipakai Collector utk melewati `auth.ScopedTenantFilter` supaya dapat agregat lintas-tenant — bukan celah aktif (actor ini tidak pernah berasal dari request), tapi preseden buruk yang berisiko ditiru di tempat lain yang authz-nya beneran penting. Fix: dihapus, diganti method usecase baru TANPA parameter `actor` sama sekali (`device.Service.PlatformStats`, `task.Service.PlatformStats/AvgCompletionSeconds/ErrorCountsByVendor`, pola yang sama seperti `session.Service.CountOpenSessions` yang sudah lebih dulu benar).

  **Batasan yang jujur, TIDAK dikerjakan (di luar scope sesi ini):** tidak ada dashboard Grafana visual (cuma raw Prometheus metrics + UI bawaan Prometheus utk lihat data/alert), tidak ada Alertmanager/notifikasi keluar (Slack/email — alert cuma muncul di tab Alerts Prometheus UI), tidak ada test `Collect()` end-to-end (`Collector` bergantung pada struct konkret usecase bukan interface sempit, jadi tidak bisa di-fake tanpa DB nyata — divalidasi lewat live testing manual berulang kali sbg gantinya, bukan unit test).

*Agent terkait: `vendor-mapping-specialist` (playbook & operasi data vendor), `rest-api-builder` (endpoint baru), `acs-security-reviewer` (audit isolasi tenant), `db-schema-guardian` (bila kuota/rate limit butuh tabel baru).*

### Fase 3 — Robustness Protokol & Skala (menandingi kematangan lapangan GenieACS)

- [ ] Uji kompatibilitas riil terhadap firmware CPE ZTE/Huawei/FiberHome/Nokia (lab test dengan perangkat fisik atau emulator resmi vendor)

  **[STARTING POINT, BUKAN SELESAI — 2026-08-23]** Katalog vendor dasar diisi via migrasi `0004_vendor_baseline_catalog`: (1) tambah **Cdata** (C-Data Technology Co., Ltd) ke `ref_vendors`, jadi total 5 vendor (ZTE/Huawei/FiberHome/Nokia/Cdata); (2) `vendor_parameter_mappings` diisi 120 baris (5 vendor × TR-098 + TR-181 × 12 logical key) untuk parameter yang path TR-069-nya benar-benar didefinisikan resmi oleh spec Broadband Forum dan identik di semua vendor by design: `device.manufacturer/model_name/serial_number/software_version/hardware_version/uptime/periodic_inform_interval/connection_request_url`, `wan.pppoe.username/password`, `wifi.ssid`, `wifi.wpa_passphrase`. Divalidasi end-to-end ke MariaDB nyata via `docker compose up -d --build` (migrasi jalan bersih, `SELECT`/`DESCRIBE` konfirmasi data masuk, down-migration teruji balik ke 0 baris lalu up lagi bersih), plus `go build`/`go vet`/`go test` via image `golang:latest` tetap lulus (migrasi data, tidak menyentuh kode Go).

  **Sengaja TIDAK diisi — jangan tambahkan tanpa dokumentasi resmi vendor atau akses device nyata (lihat komentar lengkap di file migrasi):**
  - `wifi.5g.ssid` / split 2.4G-5G — index `WLANConfiguration`/`SSID` mana yang mewakili radio 5GHz **tidak distandarkan**, beda per vendor bahkan per model/firmware.
  - `wan.ip_address` — ambigu antara `WANIPConnection` vs `WANPPPConnection` (TR-098) tergantung tipe koneksi, dan `Device.IP.Interface.{i}` (TR-181) adalah list generik tanpa index tetap untuk "yang mana WAN".
  - `device.optical.rx_power`/`tx_power` (redaman optik ONU) — di kelima vendor ini nyaris selalu lewat vendor extension nonstandar (`X_<OUI>_...`), path berbeda per vendor dan berpotensi per model/firmware. Mengisi tebakan lalu dipakai push konfigurasi ke device produksi nyata bisa merusak koneksi pelanggan — dibiarkan kosong sampai ada dokumentasi resmi vendor atau device fisik untuk konfirmasi.
  - `device_models` — tidak ada baris ditambahkan sama sekali (termasuk untuk Cdata), karena tidak ada `product_class`/nama model yang confident dari sample device nyata.
  - `vendor_ouis` — **tetap 0 baris** untuk kelima vendor (termasuk yang sudah lama ada: ZTE/Huawei/FiberHome/Nokia). OUI adalah 6 hex digit persis dari IEEE OUI registry publik; satu digit salah bisa membuat device baru silently ter-assign ke vendor yang salah saat Inform pertama, yang berisiko sama seperti path TR-069 salah (mapping parameter vendor yang keliru lalu dipakai push config). Tidak ada akses verifikasi langsung ke registry saat migrasi ini dibuat, jadi daftar OUI dari ingatan dinilai lebih berbahaya daripada kosong — perlu dilengkapi manual via Catalog Vendor UI setelah diverifikasi satu per satu dari https://standards-oui.ieee.org/.

  **Yang ini BUKAN validasi:** ini murni operasi data (insert baris, bukan percabangan kode — sesuai TECH.md §5.1), belum ada satu pun CPE fisik/emulator vendor manapun yang benar-benar Inform ke sistem ini memakai mapping ini. Item checklist di atas (uji kompatibilitas riil) tetap **belum dicentang** — starting point ini hanya mengurangi pekerjaan "isi data" yang perlu dilakukan begitu ada akses device/emulator nyata, bukan pengganti validasinya.

  **[2026-08-23]** Dokumen eksekusi `PENGUJIAN_LAPANGAN.md` dibuat untuk sesi uji fisik yang direncanakan user Senin — checklist step-by-step per device, daftar batasan yang sudah diketahui (supaya gap yang memang belum diisi di atas tidak disalahartikan sebagai bug baru saat ditemukan di lapangan), skenario yang sebaiknya dihindari di uji pertama (firmware upgrade ke device produksi, PPPoE kredensial asli), dan prosedur backup sebelum mulai. Item checklist ini SENDIRI baru akan bisa dicentang setelah sesi itu benar-benar terjadi dan hasilnya dicatat balik ke sini.
- [ ] Connection Request via STUN untuk CPE di belakang CGNAT (Fase 2 PRD) — **belum dikerjakan, sesuai CLAUDE.md ini area yang keputusan desainnya belum final, sengaja tidak diasumsikan sepihak.**
- [x] Load test: ribuan sesi CWMP concurrent, ukur p95 latency Inform→InformResponse (target NFR: <300ms) — **selesai sebagian, 2026-08-23**: tool baru `cmd/loadtest/main.go` (simulasi Inform→InformResponse→POST-kosong, konfigurasi `-sessions`/`-concurrency`/`-rate`). **Batasan jujur yang penting:** rate limiter `/cwmp` (5 req/s per IP, sengaja dipasang sbg mitigasi brute-force — lihat Fase 0) membuat tool ini TIDAK BISA mengukur "ribuan sesi concurrent" dari satu mesin/IP seperti diminta literal di item ini — burst di atas 5 req/s langsung kena `429` (diverifikasi: `-sessions 100 -concurrency 5` tanpa pacing → cuma 5 sukses, 95 kena 429). Dengan pacing `-rate 4` (di bawah limit), diuji live 300 sesi sukses 299/300, **p95 28ms** (jauh di bawah target <300ms) — tapi ini mengukur latency di batas rate limit yang disengaja, BUKAN kapasitas backend murni. Untuk angka "ribuan sesi concurrent" yang literal sesuai NFR, perlu rig multi-IP sungguhan (banyak device fisik beda IP, sesuai realita produksi) atau instance terpisah dgn rate limiter dinaikkan sementara — keduanya di luar scope yang bisa dilakukan dari satu sesi Claude Code. Dicatat sbg keterbatasan metodologi, bukan diklaim selesai penuh.
- [ ] Tuning retry/backoff task berbasis data produksi nyata (bukan asumsi)
- [ ] Strategi partisi tabel log (`device_events`, `device_optical_metrics`) setelah ada data volume produksi (TECH.md §12) — **belum dikerjakan, sesuai CLAUDE.md ini area yang keputusan desainnya belum final, sengaja tidak diasumsikan sepihak.**
- [x] Backup/disaster recovery MariaDB terdokumentasi dan **dites lewat restore drill sungguhan**, bukan cuma didokumentasikan — **selesai 2026-08-23**: prosedur `mariadb-dump`/restore didokumentasikan di `PENGUJIAN_LAPANGAN.md` §7. Drill sungguhan dilakukan (bukan cuma ditulis): dump database dev nyata (docker-compose), restore ke container MariaDB terpisah yang benar-benar baru (bukan menimpa instance yang sama — supaya drill ini murni menguji file backup, bukan cuma "database masih hidup"), verifikasi jumlah baris tabel kunci (`ref_vendors`, `vendor_parameter_mappings`, `tenants`, `users`, `activity_logs`) identik persis antara sumber dan hasil restore. Container drill dibuang setelah verifikasi.

  **Konteks kenapa ini jadi prioritas mendadak:** di tengah sesi ini, salah satu subagent (`rest-api-builder`) keliru menjalankan `docker compose down -v` (padahal dimintanya `docker compose down`) setelah validasi live-nya sendiri, dan salah asumsi "database dev ini kosong sebelum saya mulai" — padahal tidak, volume itu berisi data baseline dari sesi-sesi sebelumnya. Volume terhapus, seluruh tenant/user dev sebelumnya (`GROUPCO1`/`GROUPCO2`, `admin-t1`/`admin-t2`, dst.) hilang permanen. Tidak ada data produksi yang hilang (belum pernah ada CPE nyata connect), tapi ini insiden nyata yang langsung dikonfirmasi ke user saat terjadi, bukan disembunyikan. Skema + data hasil migrasi (vendor/mapping) berhasil dipulihkan penuh dgn `docker compose up` ulang (idempoten by design), superadmin baru dibuat via `cmd/seed-admin`. **Pelajaran:** jangan pernah minta/izinkan agent memakai `docker compose down -v` di volume dev persisten manapun kecuali eksplisit diminta user.

*Agent terkait: `cwmp-session-engineer`, `db-schema-guardian`, `task-queue-test-writer`.*

---

## 4. Cara Pakai Dokumen Ini Bersama Subagent

Subagent proyek ada di `.claude/agents/` (lihat masing-masing file untuk detail aturan):

| Agent | Fase yang paling relevan |
|---|---|
| `db-schema-guardian` | 0, 2, 3 — perubahan skema & migrasi |
| `cwmp-session-engineer` | 0, 3 — protokol & sesi CWMP |
| `vendor-mapping-specialist` | 2, 3 — operasi data vendor/parameter mapping |
| `rest-api-builder` | 1, 2 — endpoint REST baru (auth+RBAC wajib sejak awal) |
| `task-queue-test-writer` | 0, 3 — test retry/max_retries task queue |
| `acs-code-reviewer` | semua fase — review sebelum lapor selesai |
| `acs-security-reviewer` | 0, 2 — audit auth/RBAC/isolasi tenant/kredensial |
| `frontend-ux-builder` **(baru)** | 1, 2 — kerja React/Tailwind, konvensi UI |

**Gap yang baru ditutup:** sebelum dokumen ini dibuat, belum ada subagent yang meng-cover kerja frontend — padahal Fase 1 (prioritas utama user saat ini) adalah kerja frontend. Agent `frontend-ux-builder` dibuat bersamaan dengan dokumen ini untuk menutup gap tersebut.

**Alur kerja per item checklist:**
1. Pilih satu item, delegasikan ke agent yang sesuai (atau kerjakan langsung bila item lintas-domain).
2. Setelah implementasi, jalankan `acs-code-reviewer` (dan `acs-security-reviewer` bila menyentuh auth/kredensial/RBAC).
3. Tandai `[x]` di dokumen ini + catat tanggal selesai di §6.
4. Untuk perubahan skema: validasi end-to-end ke MariaDB nyata sebelum ditandai selesai (bukan opsional, lihat CLAUDE.md).

---

## 5. Definition of Done

Item baru boleh ditandai selesai hanya jika:
- Kode lulus review (`acs-code-reviewer`, ditambah `acs-security-reviewer` untuk area sensitif).
- Test relevan ada dan lulus (khusus task queue: skenario retry/`max_retries` wajib, bukan hanya happy path).
- Untuk perubahan skema: migrasi sudah dijalankan ke instance MariaDB nyata, bukan hanya dibaca sintaksnya.
- Untuk perubahan frontend: sudah dicoba manual di browser (bukan hanya lulus type-check/build).
- Tidak melanggar keputusan produk yang disengaja (mis. FR-18 — profil tidak auto re-push ke device terprovisioning).

---

## 6. Log Perubahan Dokumen

| Tanggal | Perubahan |
|---|---|
| 2026-08-21 | Dokumen dibuat. Snapshot kondisi awal dicatat, 3 fase + Fase 0 didefinisikan, agent `frontend-ux-builder` ditambahkan untuk menutup gap kerja frontend. |
| 2026-08-23 | Fase 2 item pertama selesai: White-labeling per tenant. 1 temuan tinggi (kebocoran `cwmp_inform_username` lewat endpoint baru) + 3 sedang ditemukan `acs-code-reviewer` dan diperbaiki sebelum commit. |
| 2026-08-23 | Fase 3: starting point katalog vendor dasar (migrasi `0004_vendor_baseline_catalog`) — tambah Cdata + 120 baris `vendor_parameter_mappings` TR-098/TR-181 utk parameter standar Broadband Forum. Item "uji kompatibilitas riil" TETAP belum dicentang — ini operasi data, bukan validasi ke device nyata. `vendor_ouis` sengaja tetap 0 baris (butuh verifikasi IEEE OUI registry), optical power & split WiFi 2.4G/5G sengaja tidak diisi (vendor-specific/ambigu, lihat detail di Fase 3). |
| 2026-08-23 | Persiapan uji fisik CPE Senin: `PENGUJIAN_LAPANGAN.md` dibuat, audit keamanan menyeluruh ronde 2 (1 kritis + 1 tinggi + beberapa sedang, termasuk celah privilege-escalation yang sempat lolos 2x dari fix pertama akibat case-insensitivity kolasi DB — lihat Fase 0), Fase 2 self-service tenant admin selesai (backend, dikerjakan `rest-api-builder`), load test tool (`cmd/loadtest`) + drill backup/restore sungguhan (Fase 3) selesai. **Insiden:** subagent salah jalankan `docker compose down -v`, data dev (bukan produksi) hilang, dipulihkan dari migrasi + superadmin baru dibuat — dikonfirmasi ke user saat terjadi. |
| 2026-08-23 | Fase 2: kuota task queue per tenant (`db-schema-guardian`) dan observability Prometheus sebagian (`rest-api-builder`, tanpa Grafana/Alertmanager) selesai. Kedua fitur direview `acs-code-reviewer`+`acs-security-reviewer`, beberapa temuan (index DB hilang pada jalur yg sekarang di-query tiap 15 detik, pola actor-palsu utk bypass authz, penamaan metrik gauge yg menyesatkan) diperbaiki sebelum commit. |
| 2026-08-23 | Fase 2 item terakhir selesai: object storage firmware MinIO (`rest-api-builder`) — presigned URL langsung ke CPE (keputusan user). **Fase 2 SELESAI SEMUA (8/8 item).** Direview `acs-code-reviewer`+`acs-security-reviewer`: temuan sedang-tinggi (presigned URL bocor ke role rendah lewat endpoint task generik) diperbaiki dgn redaksi field sensitif per-role di `task.Service.List/Get`, plus race condition bucket auto-create & beberapa temuan rendah lain. |
